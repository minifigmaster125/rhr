<div id = "bloggy">
    <% Post.all.each do |s| %>
    <h1><%= s.title %></h1>
    <h2><%= "Posted at #{s.date}" %></h2>
    <h3><%= s.entry %></h3>
     ----------------------------------------------------
    <% end %>
</div>

